# 轮廓仪选型工具 — 实现提示词

> **使用说明**：把以下内容发给 AI，修改 `【待你填写】` 标记的部分即可。

---

```
你是一个资深前端工程师。请按照以下要求，实现一个轮廓仪智能选型工具。

## 参考实现风格

你必须完全参照以下工具体系来实现：
- 参考工具：https://chenze3303.github.io/Code-reader-selection/
- 这是一个"海康机器人读码器选型"工具，你需要将其架构、UI风格、交互模式、算法框架完全复用到轮廓仪场景中。

## 技术约束（不可变更）

1. **纯静态单文件**：所有 HTML + CSS + JS 写在同一个 .html 文件里
2. **零依赖**：不使用任何框架、库、构建工具
3. **原生 JS**：使用 IIFE (Immediately Invoked Function Expression) 闭包模式
4. **响应式**：@media (max-width: 800px) 移动端自动切换为上下布局
5. **部署方式**：直接放在 GitHub Pages 或任意静态服务器即可运行

## UI 设计语言（严格参照参考工具）

### 整体风格
- 背景色：#f0f2f5（浅灰蓝）
- 主卡片背景：白色 #ffffff，圆角 28~40px
- 品牌强调色：【待你填写，如 #2563eb】
- 文字色：标题 #1f2c38，正文 #5d6f7e
- 边框色：#eef2f6
- 所有卡片需要 hover 时微上浮 + 阴影加深的动效
- 全局淡入动画 @keyframes fadeIn

### 布局结构
- 整体容器 max-width: 1200px，居中
- 顶部 Titlebar：Logo标题 + 提示标签
- 主体两栏：左面板 42%（参数输入），右面板 58%（结果展示）
- Modal 弹窗：毛玻璃背景 blur(4px)，白色弹窗圆角 36px，内部列表可滚动（隐藏滚动条）

### 组件风格
- 标题栏左侧有渐变文字（品牌色 + 深色渐变）
- 卡片标题左侧有 4px 宽的强调色竖线
- 表单控件（select/input）圆角 20px，聚焦时边框变色 + 发光阴影
- 主按钮：品牌色渐变 + 白色文字 + 圆角 40px + 底部阴影
- 次要按钮：透明背景 + 品牌色边框 + hover 时浅色填充
- 标签：浅灰圆角胶囊形状
- 推荐型号卡片左侧有品牌色边框标识

### 空状态
- 未输入参数时，右侧面板显示 "等待参数输入..." 灰色文字居中
- 无匹配结果时，显示黄色警告框

## HTML 结构要求

严格按照以下结构：

```
.app-window > .titlebar（logo + 状态标签）
           > .main-content > .left-panel > .card > 多个 .form-section
                           > .right-panel > .card（需求分析）+ .card（最佳推荐 + 查看全部按钮）
> .modal-overlay > .modal > .modal-header + .modal-model-list
```

## JavaScript 架构要求（严格参照参考工具）

### 核心函数签名（必须实现）

```javascript
// 1. 单位转换
function toMM(value, unit)       // mil→×0.0254, cm→×10, 默认mm
function toUM(value, unit)       // mm→×1000, 默认μm

// 2. 分辨率冗余计算（轮廓仪核心指标，类比读码器的PPM）
//    冗余因子 RF = 仪器分辨率 / 需求精度
//    RF ≥ 10 → 优秀(+30分) | 5≤RF<10 → 良好(+25分) | 2≤RF<5 → 合格(+15分) | RF<2 → 较低(-15分)
function getRedundancyScoreAndLevel(rf)

// 3. 型号遍历评分
//    对 PRODUCT_DB 中每个型号计算综合得分（5个维度，满分100）：
//    a) 分辨率满足度 (25分)
//    b) 量程适配度   (25分)：X轴满足+15，Z轴满足+10
//    c) 精度满足度   (20分)
//    d) 速度匹配度   (15分)：在线检测→高速加分，实验室→不扣分
//    e) 综合适配     (15分)：工件尺寸/重量/材质兼容
//    每个评分项附带 reasons[] 理由数组（中文描述）

// 4. 三重硬性过滤
//    过滤1：类型匹配（接触式/光学式）
//    过滤2：量程下限（X轴量程 < 需求 → 排除）
//    过滤3：工件限制（最大工件高度/重量 < 实际工件 → 排除）

// 5. 结果排序 + 过滤 + 渲染
function runSelection()        // 主入口，触发计算
function renderModalContent()  // 渲染弹窗列表（最多20款）
function resetToEmpty()        // 清空所有状态
```

### 缓存变量（IIFE 闭包内）
```javascript
let cachedFilteredList = null;
let cachedXRangeMM = null;
let cachedZRangeMM = null;
let cachedPrecisionUM = null;
let cachedProfilerType = null;
```

### 产品数据库结构 PRODUCT_DB

```javascript
const PRODUCT_DB = [
  {
    model: "【待你填写】",           // 型号名称
    brand: "【待你填写】",           // 品牌
    series: "【待你填写】",          // 系列
    type: "contact",                // 'contact'(接触式) 或 'optical'(光学式)
    xRange: 100,                    // X轴测量范围 (mm)
    zRange: 50,                     // Z轴测量范围 (mm)，注意光学式的Z范围通常用μm
    resolution: 0.01,               // 分辨率 (μm)
    accuracyZ: "±(1.5+L/200)μm",    // Z轴精度描述
    accuracyX: "±3μm",              // X轴精度描述
    raRange: { min: 0.01, max: 100 }, // 粗糙度测量范围 (μm Ra)
    measureSpeed: { min: 0.02, max: 5 }, // 测量速度 (mm/s)
    stylusTipRadius: 2,             // 触针针尖半径 (μm)，仅contact类型
    stylusForce: 10,                // 测力 (mN)，仅contact类型
    climbAngle: { up: 77, down: 87 }, // 爬坡能力 (°)，仅contact类型
    straightness: 0.2,              // 导轨直线度 (μm/60mm)，仅contact类型
    sampleMaxHeight: 150,           // 最大工件高度 (mm)
    sampleMaxWeight: 10,            // 最大工件重量 (kg)
    weight: 80,                     // 设备重量 (kg)
    interface: "USB、RS232",        // 通信接口
    protection: "IP54",             // 防护等级
    priceLevel: "standard",         // 价格级别: 'economy' | 'standard' | 'premium'
    extraFeatures: ["自动调平", "CNC多轴"], // 特殊功能
  },
  // ... 更多型号
];
```

## 用户输入参数（表单设计）

| 参数名 | 控件 | 必填 | 单位选项 | 说明 |
|--------|------|:--:|---------|------|
| 测量类型 | `<select>` | ✅ | 接触式/光学式/不限 | 硬过滤依据 |
| 应用场景 | `<select>` | ✅ | 粗糙度检测/轮廓测量/两者兼顾/3D形貌分析 | 影响速度评分权重 |
| 需求精度 | `<input>`+`<select>` | ✅ | μm/mm | 计算冗余因子 |
| X轴量程需求 | `<input>`+`<select>` | ✅ | mm/cm | 硬过滤 |
| Z轴量程需求 | `<input>`+`<select>` | ✅ | μm/mm | 硬过滤 |
| 最大工件高度 | `<input>`+`<select>` | ❌ | mm/cm | 硬过滤 |
| 最大工件重量 | `<input>`+`<select>` | ❌ | kg | 硬过滤 |
| 工件材质 | `<select>` | ❌ | 金属/塑料/玻璃/陶瓷/软质材料 | 影响类型推荐 |
| 检测节拍 | `<select>` | ❌ | 在线高速/产线常规/实验室不敏感 | 影响速度评分 |

## 右面板展示

### 卡片1：需求分析
- 计算并显示：推荐分辨率、推荐X/Z轴量程、分辨率冗余因子目标
- 格式参考：带有品牌色左边框的 .calc-box

### 卡片2：最佳推荐
- Top1 型号卡片，显示：型号名、品牌·系列、分辨率、X轴/Z轴量程、冗余因子+评级
- 下方显示评分理由标签（.reason-badge）
- "查看所有匹配型号" 按钮（无匹配时禁用）

### Modal 弹窗
- 列表展示所有通过过滤的型号（最多20款）
- 每款显示：排名、型号名、品牌系列标签、完整规格网格、冗余因子值+评级、评分理由
- Top1 卡有推荐高亮样式（左侧品牌色边框 + 浅色背景）

## 其他要求

1. 页面标题：【待你填写，如"XX轮廓仪智能选型 V1.0"】
2. 品牌渐变文字：使用品牌色实现 title 渐变效果
3. 状态提示标签："✨ 计算选型仅供参考，建议选型后实测"
4. 输入校验：必填项为空或数值≤0时，alert 提示并重置
5. 移动端适配：@media (max-width: 800px) 切换为上下布局
6. 图片区域：左侧表单内放一张说明示意图（可选，可先用 SVG fallback）
7. 无匹配结果时，显示黄色警告框列出当前过滤条件，引导用户调整参数

## 代码组织

所有代码写在一个 HTML 文件中，结构为：
1. `<style>` — 完整CSS（约500行，含响应式）
2. `<body>` — HTML结构
3. `<script>` — IIFE包裹的完整JS逻辑（含PRODUCT_DB + 所有函数 + 初始化）

## 最终交付物

一个可以直接在浏览器打开的 index.html 文件。
```

---

## 你需要修改的部分（清单）

| 位置 | 修改内容 |
|------|---------|
| `【待你填写】品牌色` | 你的品牌色，如 `#2563eb` |
| `【待你填写】标题` | 工具名称，如 "XX轮廓仪智能选型" |
| `PRODUCT_DB` 数组 | 替换为你的真实型号数据 |
| 表单中的 `<option>` | 如果你的品牌选择不同，调整下拉选项 |
| 评分权重（可选） | 如需调整各维度分值，修改 `runSelection` 中的加分逻辑 |
| 冗余因子区间（可选） | 如需调整优秀/良好/合格/较低的阈值 |
